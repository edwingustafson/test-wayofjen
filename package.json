{
  "scripts": {
    "test": "playwright test",
    "test-ui": "export DISPLAY=:99; playwright test --ui --ui-host=0.0.0.0 --ui-port=8000",
    "update": "playwright install"
  },
  "name": "test-wayofjen",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.47.2",
    "@types/node": "^22.7.0"
  }
}
